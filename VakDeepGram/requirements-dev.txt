# Development/test dependencies.
#
# Layered on top of the runtime pins rather than restating them: this file used
# to re-pin ~18 shared packages at older versions (fastapi 0.104 vs 0.128,
# starlette 0.27 vs 0.50, websockets 12 vs 16, ...), so installing it after
# requirements.txt silently downgraded the app out from under you.
-r requirements.txt

pytest==9.0.2
pytest-asyncio==1.3.0
