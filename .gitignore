# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json is deliberately NOT ignored — committing it is what makes
# `npm ci` (and a reproducible `./vak dev`) possible.

# Build outputs
dist/
build/
*.tsbuildinfo

# CDK (VakInfra has its own .gitignore for compiled *.js/*.d.ts output)
cdk.out/

# Environment files
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Temporary files
tmp/
temp/
*.tmp

# AWS
.aws-sam/
